<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<entry key="selectDailySales">
        SELECT 
		       A.SALES_DATE
		     , A.SALES
		     , A.REFUND
		     , A.TOTAL_SALES
		  FROM DAILY_SALES A
		 WHERE TO_CHAR(SALES_DATE) BETWEEN ? AND ? 
		 ORDER BY SALES_DATE
    </entry>
    
    <entry key="selectTotalSales">
        SELECT 
		       SUM(A.SALES)
		     , SUM(A.REFUND)
		     , SUM(A.TOTAL_SALES)
		  FROM DAILY_SALES A
		 WHERE TO_CHAR(SALES_DATE) BETWEEN ? AND ? 
    </entry>

</properties>